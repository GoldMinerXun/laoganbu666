<!--index.wxml-->
<view class="container">
  
  <view class='head'>
  <view class='btn btn-left' >
   问一问吧
    <image src="./q.png"></image>
 </view>
  <view class='btn'>看一看吧
  <image class='look' src="./l.png"></image>
  </view>
  </view>
  <view class='body'>
    <view class='body-wrap'>
      <view class='card-item' wx:for="{{hot}}" wx:key="{{index}}">
        <i-card class='i-card' title="{{item.title}}">
          <view slot="content" style='color:#aaa'> {{item.content}}</view>
          <view class='tag-wrap' slot="footer">
            <view class='tag' wx:for="{{item.tag}}" wx:key="{{index}}">
              <view class='tag-item'>
                {{item}}
              </view>
            </view>
          </view>
        </i-card>

      </view>

    </view>
  </view>

</view>